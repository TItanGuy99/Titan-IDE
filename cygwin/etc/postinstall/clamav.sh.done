if [ ! -f /etc/clamd.conf ]
then
    /usr/bin/mkdir -p /etc
    /usr/bin/cp /etc/defaults/etc/clamd.conf /etc/clamd.conf
fi

if [ ! -f /etc/freshclam.conf ]
then
    /usr/bin/mkdir -p /etc
    /usr/bin/cp /etc/defaults/etc/freshclam.conf /etc/freshclam.conf
fi

